//user split-by parameter if the tables doesn't have primary key column.

// Things to remember for split-by
// Column should be indexed
// Values in the field should be sparsed(not thick or densed)
// also often it should be sequence generated/evenly incremented
// it should not have null Values

sqoop import \
--options-file ./connection.props \
--table order_items_nopk \
--split-by order_item_order_id \
--delete-target-dir \
--target-dir /user/bbastola/sqoop_import/order_items

