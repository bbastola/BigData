sqoop import delimiters:

while importing data into HDFS paying attention to delimiters is very essential.

--enclosed-by <char>				Sets a required field enclosing character
sqoop import \
--options-file connection.props \
--table departments \
--target-dir=/user/bbastola/Dataset/sqoop_import/delimiters \
--delete-target-dir \
-m 1
--enclosed-by \" \
//In output file each fields will be enclosed by "

--escaped-by \\ \
//Sets the escape character \

--fields-terminated-by \| \
//Sets the field separator character |

--lines-terminated-by \: \
Sets the end-of-line with :

--mysql-delimiters
//Uses MySQL’s default delimiter set: fields: , lines: \n escaped-by: \ optionally-enclosed-by: '

--optionally-enclosed-by <char>
//Sets a field enclosing character
