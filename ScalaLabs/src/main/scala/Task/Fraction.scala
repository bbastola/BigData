package Task

/**
  * Created by bbastola on 3/9/17.
  */
class Fraction(numerator: Int, denominator: Int) {
  println("numerator is " + numerator)
  println("denominator is " + denominator)
  println("Fraction: " + numerator.toString + "/" + denominator.toString + ". Decimal: " + (numerator/denominator).toDouble)

  //def getFraction():Double
}
